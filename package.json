{
  "name": "wiz-sensor-action",
  "version": "0.0.0",
  "private": true,
  "description": "Runs the Wiz Sensor in the background.",
  "scripts": {
    "build": "ncc build main.js -o dist/main --minify --no-source-map-register && ncc build post.js -o dist/post --minify --no-source-map-register"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3"
  }
}
